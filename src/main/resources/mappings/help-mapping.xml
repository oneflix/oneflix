<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="HelpDAO">

	<select id="getHelp" parameterType="help" resultType="help">
		SELECT
		*
		FROM helps
		WHERE help_id = #{helpId, jdbcType=NUMERIC}
	</select>

	<resultMap type="help" id="helpList">
		<id property="helpId" column="help_id" />
		<result property="helpType" column="help_type" />
		<result property="helpTitle" column="help_title" />
		<result property="helpContent" column="help_content"></result>
		<result property="helpRegDate" column="help_reg_date"></result>
	</resultMap>

	<select id="getHelpList" resultMap="helpList" parameterType="help">
		SELECT * FROM helps WHERE 1 = 1
		<if test="searchHelp != null">
			AND help_title like '%'|| #{searchHelp}||'%'
		</if>
		<choose>
			<when test='helpType.equals("notice")'>
				AND help_type = 'notice'
			</when>
			<when test='helpType.equals("faq")'>
				AND help_type != 'notice'
			</when>
			<when test='helpType.equals("frequency")'>
				AND help_type = 'frequency'
			</when>
			<when test='helpType.equals("payment")'>
				AND help_type = 'payment'
			</when>
			<when test='helpType.equals("refund")'>
				AND help_type = 'refund'
			</when>
			<when test='helpType.equals("ticket")'>
				AND help_type = 'ticket'
			</when>
			<when test='helpType.equals("account")'>
				AND help_type = 'account'
			</when>
			<when test='helpType.equals("contents")'>
				AND help_type = 'contents'
			</when>
			<when test='helpType.equals("video")'>
				AND help_type = 'video'
			</when>
			<when test='helpType.equals("service")'>
				AND help_type = 'service'
			</when>
		</choose>
		ORDER BY
		help_id ASC
	</select>

	<insert id="insertHelp" parameterType="help">
		INSERT INTO helps (help_id, help_type, help_title, help_content, help_reg_date)
		VALUES((select nvl(max(help_id),0)+1 from helps),
		#{helpType,jdbcType=VARCHAR},#{helpTitle,jdbcType=VARCHAR},#{helpContent,jdbcType=VARCHAR},
		sysdate)
	</insert>

	<update id="updateHelp" parameterType="help">
		UPDATE helps
		SET
		help_type=#{helpType,jdbcType=VARCHAR},
		help_title=#{helpTitle,jdbcType=VARCHAR},
		help_content=#{helpContent,jdbcType=VARCHAR}
		WHERE
		help_id = #{helpId,jdbcType=NUMERIC}
	</update>

	<delete id="deleteHelp" parameterType="help">
		DELETE FROM helps WHERE
		help_id = #{helpId}
	</delete>

</mapper>
