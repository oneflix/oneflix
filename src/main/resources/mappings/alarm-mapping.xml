<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="#package.#mapperinterfacename">

	<select id="selectAlarmList" parameterType="alarm" resultType="AlarmVO">
		SELECT
		alarm_id,
		alarm_type,
		email,
		send_date,
		read_check,
		FROM ALARM
		WHERE alarm_id LIKE '%' ||#{word}||'%'
		ORDER BY rnum DESC
	</select>

</mapper>